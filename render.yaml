services:
  - type: web
    name: flexi-careers-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && gunicorn simple_test:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8